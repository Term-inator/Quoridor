## Agent skills

### Issue tracker

Issue 和规格以本地 Markdown 文件形式保存在 `.scratch/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五种规范化 triage 标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 single-context 领域文档布局。详见 `docs/agents/domain.md`。
